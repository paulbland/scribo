<a href="#" class="toggle"></a>
<div class="nav-content">
	Logged in as {{userProfile.name}}<br />
	<br />
	<form>

		{{#each userOptions}}
			<fieldset>
				<legend>{{first_to_upper @key}}</legend>
				{{#each this}}
					<label><input type="radio" name="{{@../key}}" value="{{this}}" {{#check_prop ../../userPrefs @../key this}}checked{{/check_prop}}> {{first_to_upper this}}</label><br /> 
				{{/each}}
			</fieldset>
			<br />
		{{/each}}

		{{!-- {{#each userOptions}}
			<fieldset>
				<legend>{{first_to_upper name}}</legend>
				{{#each vals}}					
					<label><input type="radio" name="{{../name}}" value="{{this}}" {{#check_prop ../../userPrefs ../name this}}checked{{/check_prop}}> {{first_to_upper this}}</label><br /> 
				{{/each}}
			</fieldset>
			<br />
		{{/each}} --}}

	</form>
	<br /> 
	{{!-- My projects:<br />
	<a href="#" onclick="return confirm('Are you sure you wish to switch projects?')">Project 1</a><br />
	<a href="#" onclick="return confirm('Are you sure you wish to switch projects?')">Project 2</a><br /> --}}
	<br />
	<a href="#" class="logout">View Tutorial</a>
	<br />
	<br />
	<a href="#" class="logout">Logout</a>
</div>
<div class="nav-logo">S<span>cribo</span></div>